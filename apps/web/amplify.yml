version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - corepack enable
            - corepack prepare pnpm@9.15.0 --activate
            - pnpm install --frozen-lockfile
        build:
          commands:
            - cd ../..
            - pnpm --filter @mcpsearch/shared build
            - pnpm --filter @mcpsearch/web build
            # Copy static files to standalone directory (use || true to handle missing folders)
            - cp -r apps/web/public apps/web/.next/standalone/apps/web/public 2>/dev/null || mkdir -p apps/web/.next/standalone/apps/web/public
            - cp -r apps/web/.next/static apps/web/.next/standalone/apps/web/.next/static
      artifacts:
        baseDirectory: .next/standalone
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
          - ../../apps/web/node_modules/**/*
          - ../../packages/shared/node_modules/**/*
          - ../../apps/web/.next/cache/**/*
    appRoot: apps/web
